# APP.MAIN

The application mounts the top-level view.

# APP.VIEW

The top-level view renders the primary button component.

# APP.BUTTON

The primary button formats its visible label through shared code.
